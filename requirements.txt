# OverlayX - Python Dependencies
# ================================

# Core dependencies
pyvirtualcam>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0
psutil>=5.9.0
Pillow>=10.0.0
PyYAML>=6.0

# MediaPipe 0.10.x for background segmentation
mediapipe>=0.10.0

# Menu bar app
rumps>=0.4.0      # macOS (NSStatusBar nativo)
pystray>=0.19.0   # Linux / Windows (system tray)
